import 'package:flutter/material.dart';

class DriverMapPage extends StatelessWidget {
  const DriverMapPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('DRIVER_MAP_PAGE'),
      ),
    );
  }
}
